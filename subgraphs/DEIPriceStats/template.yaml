specVersion: 0.0.4
description: Analytics for the Prices.
repository: https://github.com/deusfinance/ecosystem-subgraphs/tree/main/subgraphs/DEIPriceStats
schema:
  file: ./schema.graphql
dataSources:
  - kind: ethereum/contract
    name: Vault
    network: '{{ network }}'
    source:
      address: '{{vaultAddress}}'
      abi: Vault
      startBlock: {{vaultStartBlock}}
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.6
      language: wasm/assemblyscript
      file: ./src/mapping/index.ts
      entities:
        - DeiTokenPriceSnapshot
        - LatestDeiTokenPriceSnapshot
      abis:
        - name: Vault
          file: ./abis/Vault.json
      eventHandlers:
        - event: Swap(indexed bytes32,indexed address,indexed address,uint256,uint256)
          handler: handleSwapEvent
